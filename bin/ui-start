#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
docker compose \
  --env-file "$ROOT/.env" \
  -f "$ROOT/traefik/docker-compose.yml" \
  -p local-infra \
  up -d --build control-panel

echo "Control panel iniciado: http://infra.localhost"
