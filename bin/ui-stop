#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
docker compose \
  --env-file "$ROOT/.env" \
  -f "$ROOT/traefik/docker-compose.yml" \
  -p local-infra \
  stop control-panel

echo "Control panel detenido."
